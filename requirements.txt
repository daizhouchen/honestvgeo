# CoGEO reproduction dependencies.
# Pinned versions will be filled in after the first `pip install` succeeds.
open-clip-torch
piq
diffjpeg
einops
numpy
pillow
scikit-image
huggingface-hub
transformers
accelerate
